apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  type: ClusterIP 
  selector:
    appType: frontend
  ports: 
    - name: http
      port: 80 # Service Port
      targetPort: 3000 # Container Port